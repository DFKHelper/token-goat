pre-commit:
  commands:
    lint:
      run: uv run ruff check {staged_files}
      glob: "*.py"

pre-push:
  commands:
    typecheck:
      run: uv run mypy src
    test:
      run: uv run pytest -q --tb=short
