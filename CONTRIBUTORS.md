# Contributors

## eSaadster

The TypeScript rewrite was directly inspired by [pi-token-goat](https://github.com/eSaadster/pi-token-goat), eSaadster's fork that explored session-aware token compression under Raspberry Pi memory constraints. The core insight — track what the session has already seen and intercept redundant reads before they hit the model — originated in that work.
